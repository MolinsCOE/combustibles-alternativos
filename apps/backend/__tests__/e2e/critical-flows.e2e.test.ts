import { test } from "@playwright/test";

test.describe("critical flows", () => {
  test.skip("login flow", async () => {
    await Promise.resolve();
  });

  test.skip("create asset flow", async () => {
    await Promise.resolve();
  });

  test.skip("search flow", async () => {
    await Promise.resolve();
  });
});
